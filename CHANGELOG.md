# Changelog

## [4.0.0] - 2026-09-03

> 4.0 是统一版本号后的首个大版本（原 3.5.x 系列全部归入此版）。md2wechat 与 markdown2anything 同步发布。

### 知乎图片链路彻底修复
- **彻底修复复制到剪贴板图片"不知去向"**：真相是图片其实上传到了知乎图床，但我们缺了关键的第 3 步——OSS 上传后知乎后台**异步处理图片**，必须再 `GET /images/{image_id}` 轮询等 `status=success` 才返回**真实 CDN URL**（`src`）。之前没轮询，只能硬编码 `pic4.zhimg.com` 猜 URL → 粘贴进编辑器加载失败。现在按 zhihu-cli 完整流程：注册 → OSS 上传 → 轮询拿真实 `src`
- **图片去重缓存**：同一张图（按内容 md5）不再重复上传，命中缓存直接复用 CDN URL，持久化到 globalState（上限 500 条）
- **未登录引导**：复制含图片内容但未登录知乎时，弹窗询问「现在去登录知乎吗」
- **彻底修复自动发布图片位置错乱（跑到文末/丢失）**：知乎编辑器是 Draft.js，粘贴会剥离自定义属性导致占位替换失效。改为最稳方案：先通过 file input 全部上传并回读真实 URL → **清空编辑器** → 把真实 URL 回填进干净 HTML **整篇重贴**，图片位置由 HTML 顺序天然决定

### 封面
- **封面按钮提到发布区**：从「更多」菜单移出，与微信/知乎/小红书/Twitter 四个社交平台同级
- **内置 6 套预设背景**：梦境蓝紫 / 落日橙紫 / 极简灰白 / 墨绿森林 / 夜空繁星 / 清新薄荷，没背景图也能一键出封面；预设可删除（重启自动恢复），自定义背景删除则物理移除
- **背景选择支持「全局默认 / 仅当前文章」**：全局默认所有文章生效；仅当前文章只对本篇生效（per-md 覆盖，切换文件自动识别）
- **封面支持微信规格**：小红书 1080×1440 / 微信首图 1080×460 / 微信缩略图 900×900，各类型排版独立记忆

### 预览与设置
- **默认主题改为 Claude 风格**：暖色衬线排版（橙系强调、暖白底），开箱即用；仍可一键切换其他主题
- **正文宽度改为百分比滑块**：50%~100% 滑动条按预览区宽度比例控制正文宽度，适配不同屏幕；拖动实时预览、松手才写入全局默认。100% 即真正全宽铺满（修复"全宽没变化"——原来被外层 680px 上限卡住）
- **新增「设置」面板**：正文宽度、表格显示、自定义 CSS 等；支持「仅当前页面生效」或「写入全局默认」
- **表格智能自适应**：窄表收缩居中，长 URL/连续字符防撑高行，表头 nowrap + 斑马纹 + 悬停高亮

### 安全
- **Cookie / API Key 全部走系统钥匙串**（SecretStorage）：不落盘明文、不写进配置或日志；登出即删凭证，旧版明文缓存自动迁移清理

## [3.5.5] - 2026-09-03

### 安全增强
- **Cookie 全部改为系统钥匙串加密存储**：知乎、小红书、Twitter 的登录 Cookie 不再明文存本地文件，统一迁移到 VS Code SecretStorage（macOS 钥匙串 / Windows 凭据管理器 / Linux libsecret），与 API Key 同等安全级别。升级后首次启动自动把旧明文 Cookie 迁移进钥匙串并清理，无需重新登录
- **修复知乎自动发布图片全部"上传失败"**：之前把本地图片用临时本地图床（http://127.0.0.1）暴露给知乎，而知乎编辑器是【后端】抓取远程图、访问不到用户本机 → 编辑器里所有图片显示"上传失败"。改为走编辑器自己的上传通道：正文图片先用占位符粘贴，再逐个把本地图片喂给编辑器的图片上传控件（input[type=file][accept=image/*]），让知乎自己上传并回读 zhimg CDN 地址替换占位符（与小红书自动发布同一套做法）

## [3.5.3] - 2026-09-02

### 新增
- **配置列表分组折叠**：LLM 配置按「平台 > API Key」分组的标题可点击折叠/展开，配置多的时候一眼收拢，状态会记住不随刷新丢失
- **一键导出配置**：把所有已保存的 LLM 配置导出为 JSON 备份（只含名称/接口/模型/Key 指纹，**不含密钥明文**），方便换电脑迁移
- **导入配置**：粘贴导出的 JSON 即可恢复；未带 Key 的配置会自动继承同平台（同主机）已有的 Key，陌生平台留空待填
- **🔑 查看 Key**：每张配置卡有钥匙按钮，可查看该配置在系统钥匙串里存的完整 Key（只在本机读取展示，不落盘不传输）
- **⚡ 全部测试**：一键批量测试所有已保存配置的连接，逐个显示结果与进度

### 优化
- **包体积再减 39%**（18.95MB → 11.07MB）：排除无用 map/文档/类型声明，mathjax 无障碍语音模块、playwright 调试视图等冗余，安装更快
- **测试体系正式化 + CI**：源码/WebView 语法检查、面板渲染、LLM 配置流程等回归测试全部纳入 `npm test`，push 后 GitHub Actions 自动跑（含 Playwright UI 测试）
- **发布自动化**：`npm run release` 一键打包→GitHub Release→市场发布；推送 `v*` tag 也会自动构建发布
- **内部重构**：WebView 面板从扩展代码里的超长模板字符串拆成独立 `webview/panel.html`（模块边界清晰，彻底告别转义类偶发 bug）

### 修复
- **所有侧面板支持拖拽调宽**：LLM 配置、自定义样式等面板左侧新增拖拽条，按住左右拖动即可调整面板宽度（小红书面板原有，现在全面板通用）；拖拽后的宽度会被记住，关掉再打开保持不变，双击拖拽条恢复默认宽度；最大宽度受限，确保预览区始终可见
- **修复"脚本合成封面"失败**：扩展宿主跑 Node 脚本（封面合成、小红书截图、知乎登录等）时 `process.execPath` 是 VS Code 的 Electron 二进制，必须带 `ELECTRON_RUN_AS_NODE=1` 才会执行脚本，否则静默失败并提示"封面生成失败"。已修复全部 8 处 spawn 调用并加回归测试
- **修复知乎复制按钮一直显示"处理中"**：`btn-zhihu` 被重复绑定了两个点击处理器，后一个用 `textContent` 直接覆盖按钮内容，把 `.item-label` 元素销毁，导致收到复制结果后按钮无法恢复文案。已删除重复绑定（小红书长文复制按钮同样问题一并修复）
- **修复导出微信公众号 HTML 后公式消失**：导出路径（导出 HTML 文件）曾用 KaTeX HTML + 去掉 @font-face 的 CSS，粘贴到没有 KaTeX 字体的网页后公式渲染为空白。现在导出路径与复制路径一致，公式全部转成内联 SVG（mdnice 格式），任何浏览器/网页都能正常显示；复杂公式（矩阵/求和/希腊字母等）也不会降级为外部图片链接
- **修复知乎/微信粘贴后图片不显示**：远程 URL 图片粘贴到知乎/微信编辑器会被拒绝（编辑器只认站内/内联图片）。现在复制内容时自动把远程图片下载并内联为 base64，粘贴后图片直接显示，无需手动上传；下载失败的图片保留原链接不阻塞复制
- **修复知乎/微信粘贴后图片上下多余空行**：markdown 空行渲染成的空段落不再残留，图片（含带说明文字的 figure）前后的空行全部清除，移动端阅读不再需要手动删空行
- **修复公众号公式 `\bm` 不渲染**：MathJax 发行版不含 bm 宏包，`\bm` 被当作未知命令、公式里出现红色 "bm" 文字（预览用 KaTeX 正常，复制到公众号才暴露）。已手动加载 boldsymbol 宏包并把 `\bm` 定义为 `\boldsymbol`，粗体公式现在正确渲染
- **修复知乎复制图片仍不显示**：知乎编辑器只认自家图床（zhimg.com）的图片，base64/外链都会被拒。现在复制知乎内容时若已登录知乎，自动把图片上传到知乎图床并替换为 CDN 链接，粘贴即可显示；未登录或上传失败的图片保留原样并提示
- **修复知乎自动发布图片全部"上传失败"**：之前把本地图片用临时本地图床（http://127.0.0.1）暴露给知乎，而知乎编辑器是【后端】抓取远程图、访问不到用户本机 → 编辑器里所有图片显示"上传失败"。改为走编辑器自己的上传通道：正文图片先用占位符粘贴，再逐个把本地图片喂给编辑器的图片上传控件（input[type=file][accept=image/*]），让知乎自己上传并回读 zhimg CDN 地址替换占位符（与小红书自动发布同一套做法）

## [3.4.7] - 2026-08-28

### 新增
- **免费模型列表缓存 7 天**：OpenRouter 免费模型列表拉取一次后缓存 7 天，不再每次打开都重新请求；面板里显示「缓存于 X 分钟/小时前」，点「↻ 刷新」强制重新拉取
- **配置三级分组展示**：已保存的配置按「平台 > API Key > 模型」分组（如 openrouter.ai 平台下，同一把 key 的所有模型归一组、不同 key 分不同组），一眼看清哪些配置共用一把 key
- **保存所有历史配置**：每个配置独立唯一 ID，不再互相覆盖（之前同一预设/自定义会互相顶掉，只能留两个）

### 修复
- **「测试」Key 复用扩展为同平台**：之前 Key 只按完全相同的接口地址复用，换模型/新建同平台配置后仍显示「无 API Key」；现在同一平台（同一主机名）自动继承已有 Key，跨平台仍不复用
- **清除 Key 指向正确配置**：现在会清除当前编辑中（或正在使用）配置的 Key，而不是按预设名随便清除
- 切换配置时自动退出「修改」编辑态，避免误把表单内容存进旧配置

## [3.4.6] - 2026-08-28

### 修复
- **测试连接 Key 复用补全**：表单测试时 Key 留空，除了复用同接口地址已有配置的 Key，还会回退到历史遗留扁平配置（旧版 `llm.apiKey`）里的 Key，避免"换模型测试读不到旧 Key"再报缺 Key

## [3.4.5] - 2026-08-28

### 修复
- **「测试」按钮改用表单当前值**：之前测试的是「当前激活的已保存配置」（旧版 deepseek），所以在表单里换新模型再点测试仍是旧模型报错。现在直接测试表单里填的接口地址 + 模型名（未保存也能测），Key 留空自动复用同接口地址已有 Key
- **删除配置按钮真正可用**：VS Code webview 禁用 `window.confirm()`，导致删除确认永远不通过、删除按钮形同虚设。改为「再点一次删除」二次确认
- **历史遗留扁平配置可见可删**：早期版本存入 `llm.baseUrl`/`llm.model` 的旧配置（不在配置列表里、删不掉、还会被默认读取）现在显示为「历史遗留配置」卡片，可一键删除
- **免费模型点选后自动测试**：从「🎁 OpenRouter 免费模型」列表点选某个模型后，立即用该模型测一次连接，马上看到能不能用

## [3.4.4] - 2026-08-28

### 新增
- **OpenRouter 免费模型一键挑选**：LLM 配置面板新增「🎁 OpenRouter 免费模型」按钮，自动拉取官方当前可用的免费模型列表（带 `:free` 后缀），点选即自动填好接口地址 + 模型名
- **接口报错可操作化**：OpenRouter 免费档不可用（unavailable for free）或模型不存在（not found）时，报错会附带中文操作建议——提示免费模型需以 `:free` 结尾、可点「免费模型」按钮挑选，并附上官方建议的模型名

### 修复
- 修复 LLM 配置面板脚本语法错误（免费模型列表的悬停样式改用 CSS，不再内联事件导致页面脚本解析失败）

## [3.4.3] - 2026-08-28

### 修复
- **修复 LLM 配置保存报错「没有注册配置」**：`llm.profiles` / `llm.activeProfile` 两个配置键此前未在扩展清单注册，导致「保存并使用」写入配置时抛错、整条保存中断。已在 package.json 注册这两个键
- **修复 API key 从未真正保存**：由于保存流程在中途抛错，key 一直没写入系统钥匙串，看起来「填过不复用」。保存恢复正常后 key 会持久化

### 新增
- **同接口地址 Key 自动复用**：同一端点（如 OpenRouter）填过一次 key 后，新建同 baseUrl 配置、只换 model 时留空 key 即可自动继承已有 key，不用重复填
- **配置卡片显示 Key 指纹**：每套配置展示其使用的 Key 指纹（如 `🔑 sk-or…aB3f`），同一接口地址存多把不同 key 也能一眼区分

## [3.4.2] - 2026-08-28

### 新增
- **图标按钮悬浮提示**：工具栏紧凑图标按钮（目录/缩放/同步/表格等）悬停 1 秒后显示功能说明，不再需要猜图标含义
- **LLM 配置卡片 + 连接状态**：每个已保存的配置变成一张可点击卡片，显示模型/接口地址 meta 信息与连接状态徽标（未测试 / 测试中 / ● 连接成功 / ● 连接失败），整卡点击即切换配置，每卡带「测试连接」按钮即时验证
- **下拉菜单防溢出**：导出 / 更多等下拉菜单在窄窗口/换行时自动收进视口，不再向左溢出看不到

## [3.4.1] - 2026-08-28

### 优化
- **平台按钮图标换新**：微信（双气泡 SVG）/ 知乎（知）/ 小红书（RED）/ Twitter（X）改用干净的品牌字形，去掉高饱和彩色 emoji
- **配色整体压暗**：发布平台按钮改为暗色调（深底 + 品牌色描边/文字），不再高饱和实底刺眼；「一键导出」主 CTA 用主题色暗化实底（`--m2a-accent-dim`）；面板打开高亮、`.btn-primary`/`.btn-secondary` 同步调暗
- **工具收纳为「⋮ 更多」菜单**：LLM 配置 / 自定义样式 / 封面生成 收进工具栏右侧下拉菜单，工具栏更精简

## [3.4.0] - 2026-08-28

### 优化
- **工具栏三区布局重构**：预览控制（主题/目录/缩放/同步/表格）收敛为紧凑图标按钮簇；四个发布平台（微信/知乎/小红书/Twitter）放大加粗作为主操作；导出/LLM/样式/封面收进右侧工具区。窄窗口整区换行不再零散
- **小红书界面跟随主题色**：主 CTA「一键导出」用当前主题强调色实底、「生成预览」描边幽灵按钮；区块标题加主题色竖条；参数/预览图折叠块升级为卡片式 accordion；输入框聚焦描边、预览图缩略图圆角卡片感均跟随主题强调色（`--m2a-accent`，由 `applyTheme` 按 14 套主题注入）

## [3.3.8] - 2026-08-28

### 修复
- **表格模式初始化防御**：`vscode.getState` 读取包 try/catch，任何环境异常都不会再阻断 webview 初始化，避免按钮点击完全失效
- **工具栏显示运行版本**：预览左上角显示 `v3.3.8` 徽标，便于确认当前实际加载的是哪个版本（排查旧版本/多版本并存问题）

## [3.3.7] - 2026-08-28

### 新增
- **表格显示模式切换按钮**：预览工具栏新增 `表格·滚动 / 表格·展开` 开关——默认横向滚动（宽表格在正文内滚动，不撑宽排版），点击可切到完整显示（宽表格自然展开，预览区横向滚动看全表）；仅影响预览，不影响导出 HTML；文档含表格时按钮自动出现

## [3.3.6] - 2026-08-27

### 修复
- **宽表格不再撑爆预览/导出页面**：`<table>` 自动包进 `overflow-x:auto` 滚动容器，正文 100 宽、表格 200 宽时表格在容器内横向滚动，无需再缩放页面看全表；微信/知乎/小红书/完整 HTML 导出同步生效

## [3.3.5] - 2026-08-27

### 新增
- **小红书封面**：工具栏 `🖼️ 封面` 入口，支持脚本固化（1080×1440 纯文字白描边直贴，不挡背景）+ LLM 生背景（去标题，蜡笔小新/猪猪侠中央留白 prompt → `/images/generations` 生图，失败自动复制 prompt）
- **封面可视化排版**：270×360 等比预览（1080×1440 scale 0.25 所见即所得），标题可拖拽、滚轮（按住 Ctrl）/滑条调字号与宽度，`✓ 确认排版` 存为默认值，`⬇ 导出高清封面` 直出
- **底图历史**：上传即设为默认，历史墙保留最近 20 张，点选切换/✕ 删除，持久化于 `globalStorage/cover`
- **标题完整显示**：去掉 3 行截断，超长标题自动换行完整展示
- **触控板优化**：预览区滚轮缩放仅按住 Ctrl/Cmd 时生效，避免双指滚动误触

### 优化
- **小红书自适应跟随主题**：`adaptive` 模式新增 `☑ 跟随预览主题色`（`xhs.adaptiveUseTheme` 默认开），基于 14 套主题的强调色生成 `--xhs-accent` 变量，关闭则固定小红书红

### 修复
- 修复 webview `await` 在非 async 回调中导致预览卡死
- 修复打包漏 `node_modules` 导致 `Cannot find module 'marked'`

## [3.1.0] - 2026-08-12

### 侧边栏面板重设计
- **小红书 / Twitter 面板三段式布局**：拆分为「导出」「文案」「账号 & 发布」三个模块，深浅背景分层，区分度更高，不再一屏堆满
- **折叠式次级操作**：参数设置、预览图、更多操作、手动 Cookie 均改为可折叠区块，减少滚动
- **登录状态、发布进度归位**：登录/过期提示放入「账号 & 发布」，AI 生成进度放入「文案」

### LLM 配置
- **多配置卡片式管理**：已保存的每套配置以卡片展示（配置名 + 模型 + 域名 + Key 状态），支持一键「切换使用」「编辑 / 重命名」「删除」
- **重命名支持**：可给同一平台的多套配置起名（如「OpenRouter 免费」「OpenRouter 付费」），互不覆盖
- **快速预设切换即时填充**：切换预设后接口地址、模型名自动填入——优先回填该预设上次保存的配置，否则用推荐默认值（含免费模型）

### 修复
- **修复社交面板整块脚本失效**：webview 模板字符串中一处正则转义（`\/`）被塌缩导致整个社交/LLM 脚本块解析失败，表现为快速预设切换后接口地址、模型名不更新
- **修复 VSIX 缺失依赖**：打包时改为包含 `node_modules`，解决运行时 `cannot find module pngjs`

---

## [3.0.4] - 2026-08-11

### 新增
- **LLM 多配置管理**：支持同时保存多套 LLM 配置（平台 + 模型 + Key），一键切换，API Key 加密存储，无需重复输入
- **硅基流动预设**：LLM 快捷预设新增硅基流动（SiliconFlow），切换预设自动填充接口地址和模型名
- **小红书双模式参数独立控制**：默认截图模式提供「字体缩放（%）」，自适应模式提供「正文字号（px）」，两种模式参数互不干扰
- **参数记忆**：「★ 设为默认」将当前参数保存为该模式专属默认；「↺ 出厂值」还原系统初始参数
- **面板 UI 重设计**：小红书 / Twitter 面板各模块（导出模式 / 账号 / AI 生成）用彩色边框卡片区分；参数区、预览图区均可折叠，减少滚动距离

### 修复
- 小红书 AI 文案解析兼容性提升：兼容模型返回数组格式 JSON、字符串内含换行的 JSON；报错时展示完整原始返回
- 修复一键导出时重复生成两份同内容图片（`xhs_NN.png` + `xiaohongshu-NN.png`）
- 修复 LLM 预设切换后接口地址不自动填充的问题
- 修复社交平台按钮点击后图标颜色变蓝的问题

---

## [3.0.3] - 2026-08-08

### 新增
- **小红书双模式导出**：支持保留当前主题的默认 HTML 页面截图，以及面向手机阅读的 3:4 自适应截图
- **移动端排版优化**：自适应模式同步放大标题、正文、代码、公式、表格和图片间距

### 优化
- 导出模式自动保存，一键导出和发布时自动生成配图保持一致

---

## [3.0.2] - 2026-07-21

### 修复
- **工具栏自适应换行**：预览窗口较窄时按钮不再被裁掉，自动换行显示

---

## [3.0.0] - 2026-07-14

### 新增
- **导出 PPT**：三种 backend（Slidev / Marp / Pandoc），依赖自动安装
- **导出 Word**：pandoc，保留代码 / 公式 / 图片，可编辑
- **PPT AI 改写**：LLM 重写为 PPT 格式，三 backend 各有专属 prompt，带版本管理
- **统一 LLM 配置**：工具栏集中配置，不再分散各平台面板
- **局部缩放**：Ctrl+滚轮以鼠标为锚点局部放大
- **编辑器↔预览双向定位**
- **主题增至 14 套**：新增 Claude Pro / Medium 编辑 / 简约蓝 / 科技博客
- **图片空行优化**：复制到微信/知乎时自动清理多余空行

### 优化
- 工具栏按平台聚合为下拉菜单，17 个平级按钮 → 9 个入口
- 样式面板内置速查表和 AI 提示语
- HTML 导出默认同目录，可自定义路径

---

## [2.8.x]

- 知乎发布重做（Playwright 真实浏览器）
- 小红书 / Twitter 自动发布 + AI 文案 + 版本管理
- 预览区图片悬停可复制（真 PNG 位图）
